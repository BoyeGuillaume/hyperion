[build-system]
requires = ["maturin>=1.11,<2.0"]
build-backend = "maturin"

[project]
name = "hypi"
requires-python = ">=3.8"
license-files = ["LICENSE"]
version = "0.1.0"

[tool.maturin]
python-source = "python"
module-name = "hypi._sys"
