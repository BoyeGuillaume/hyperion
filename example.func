define cc i32 %pow(%a: i32, %n: i32) {
entry:
    %is_n_null = icmp eq i1 %n, i32 0
    branch %is_n_null, label output, label loop

loop:
    %x = iadd wrap signed i1 %x, %x
    ret void

output:
    ret i32 1
}

